# Session Log — 2026-05-17

## Goal

Fix tiling window resize snap-back bug and preferences default-ON bug, with thorough E2E test
coverage across layouts, constraint states, and directions.

## Fixed bugs

| Bug                                                                            | File                       | Fix                                          |
| ------------------------------------------------------------------------------ | -------------------------- | -------------------------------------------- |
| `resizePairForWindow.nodeValue` cast assumes `Meta.Window` but can be `St.Bin` | `window.ts` ~3139          | Guard with `isWindow()` before `get_title()` |
| `index! +/- 1` OOB in `diffParent` branches                                    | `window.ts` 3182-3248      | Bounds check on index                        |
| GNOME 50 proxy blocks `ext.getSettings()`                                      | `window.ts`, `commands.js` | `global.__anvil_settings` in `enable()`      |
| GNOME 50 proxy blocks `ext.settings`                                           | `commands.js`              | Use `ext.getSettings()` instead              |
| `_resizedWindows` race with Wayland `size-changed`                             | `window.ts`                | `Set` → `Map<number, number>`, count ≥ 2     |
| `isOrthogonalResize()` heuristic fails                                         | `resize.js`                | Tree `isHSplit()` / `isVSplit()` heuristic   |
| `_resizedWindows` state bleed between tests                                    | `commands.js`, `resize.js` | `clearResizedWindows()` in `beforeEach`      |

## Test results

| Suite       | Before | After   |
| ----------- | ------ | ------- |
| E2E total   | 21/81  | 81/81   |
| Integration | ~35    | ~115+   |
| Unit        | —      | 801/801 |

## Test configuration

```
gnome-shell --wayland --headless --virtual-monitor 1920x1080
Work area: x=0 y=32 w=1920 h=1048
Constraint limit: 0.35 × 1920 = 672 width, 0.35 × 1048 = 366 height
Devkit connector: "Meta-0"
```

## Key files

- `src/lib/extension/window.ts` — resize, constraints, `_resizedWindows`
- `src/extension.ts` — `global.__anvil_extWm`, `global.__anvil_settings`
- `test/e2e/lib/commands.js` — test helpers
- `test/e2e/suites/resize.js` — 74 data-driven resize tests
